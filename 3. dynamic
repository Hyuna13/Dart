dynamic은 꼭 필요한 상황에서만 사용

어떤 타입인지 확실치 않을때 => 여러 타입을 가질 수 있는
아무런 값을 지정하지 않거나, 타입을 선언하지 않으면 자동적으로 dynamic 타입을 가진다

void main() {
  dynamic = name;
  if(name is String) {
    
  }
  if(name is int) {
    
  }
}
